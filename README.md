# bldc_console
An Electron + React dashboard project.
## Scripts
`npm run dev` - Starts the Vite dev server and the Electron app.
`npm run electron:build` - Builds the React app and packages the Electron application.

```
sudo usermod -a -G dialout $USER
```